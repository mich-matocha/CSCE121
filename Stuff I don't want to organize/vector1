#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main()
{
	vector<int> v;
	v.push_back(7);
	v.push_back(11);
	for(decltype(v.size()) i = 0; i < v.size(); ++i)
	{
		cout << v.at(i) << endl;
	}

	return 0;
}